# GLASS environment without PyRosetta (Biotite-only analysis)
# Use with: python -m venv .venv_biotite && .venv_biotite/bin/pip install -r requirements-biotite.txt

pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0
adjustText>=0.7
biotite
biopython>=1.79
snakemake>=7.0.0
pytest>=7.0.0
